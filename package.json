{
  "name": "magic-reverse-proxy",
  "version": "5.0.0",
  "description": "HTTP/HTTPS reverse proxy with a fluent TypeScript API, virtual hosts, WebSocket support, pluggable Round-Robin balancing, and HSTS policy helpers — zero Express/http-proxy dependencies.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MurylloEx/Magic-Proxy.git"
  },
  "keywords": [
    "proxy",
    "reverse-proxy",
    "websocket",
    "round-robin",
    "vhost",
    "hsts",
    "load-balancer",
    "typescript"
  ],
  "author": "Muryllo Pimenta de Oliveira",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/MurylloEx/Magic-Proxy/issues"
  },
  "homepage": "https://github.com/MurylloEx/Magic-Proxy#readme",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/ws": "^8.18.1",
    "tsc-alias": "^1.9.5",
    "typescript": "^5.8.3",
    "vitest": "^4.1.11",
    "ws": "^8.21.3"
  }
}
